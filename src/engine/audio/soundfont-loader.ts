/**
 * Handles SoundFont downloading, caching (Service Worker + Cache API),
 * and loading into the Synthesizer.
 */

// TODO: Implement
// - loadBundledSoundFont(): Load GeneralUser GS from public/soundfonts/
// - downloadAndCacheSoundFont(url, id): Fetch, cache, return ArrayBuffer
// - isCached(id): Check if a SF is already in cache
// - getCachedSoundFont(id): Load from cache
export {};
