/**
 * Manages the shared AudioContext singleton.
 * Handles browser autoplay policy (resume on user gesture).
 */

// TODO: Implement
// - Create AudioContext lazily
// - Resume on first user interaction
// - Provide context to audio engine
export {};
